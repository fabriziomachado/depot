<h2>Your Pragmatic Cart</h2>
<ul>    
  <% @cart.line_items.each do |item| %>
   <li><%= item.product_id %> - <%= item.quantity %> &times; <%= item.product.title %></li>
  <% end %>
</ul>

<%= link_to 'Back', store_index_path %>
